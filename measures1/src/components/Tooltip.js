import React, { Component } from 'react'

export default class Tooltip extends Component {
    render() {
        return (
            <div>
                <button>Open</button>
            </div>
        )
    }
}
